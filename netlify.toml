[build]
  publish = "."
  functions = "netlify/functions"

[functions."refresh-strava-cache"]
  # Netlify cron schedules run in UTC; 04:00 UTC is midnight Eastern during daylight saving time.
  schedule = "0 5 * * *"
